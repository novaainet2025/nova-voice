# 개선 노트 — nova-voice
> 생성: 2026-07-05T22:00 | 브랜치: main
> 이전 노트: 없음
> 커밋: 76d1e6d a5dc825 f215c91 f2d1fde f735d6e 

### ✅ 이번 작업 요약
TTS 청크 파이프라인 최적화 및 노이즈 제거로 실시간 음성 처리 품질 향상.

---

### 🔄 Before → After (이번 세션에서 개선된 사항)
| 이전 문제 | 이번 해결 | 파일/코드 |
|------|------|------|
| TTS 청크 간 불필요한 갭 발생 | 1-ahead + dynamic merge로 청크 병합 최적화 | `tts-client.ts`, `pipeline.ts` |
| NCO API 30초 타임아웃으로 인한 장문 처리 실패 | 타임아웃 120초로 증가 및 중복 요청 제거 | `nco-client.ts`, `tts-client.ts` |
| PTY 노이즈로 인한 TTS 재생 품질 저하 | 노이즈 필터링 알고리즘 추가 및 Stop 훅 개선 | `whisper.ts`, `ipc.ts` |
| 설정 패널 UI 반응성 문제 | `useRecorder` 훅과 상태 관리 최적화 | `SettingsPanel.tsx`, `useRecorder.ts
